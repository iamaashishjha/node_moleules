// /* eslint-disable camelcase */

import { Stb, LauncherStore } from "../config/types";

/* eslint-disable camelcase */
const LauncherStore: LauncherStore = {
    box_model_prefix: new Map(),
    hotspots: new Map(),
};

export default LauncherStore;
